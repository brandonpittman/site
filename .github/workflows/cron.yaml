name: Refresh Pinboard unread links
on:
  push:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call API route
        run: curl --url 'https://blp.is/api/pinboard/cache-links'
